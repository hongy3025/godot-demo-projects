# 使用方法

要在其他项目中使用这些插件，请将任意一个文件夹
复制到 Godot 项目的 `addons/` 文件夹中。

例如，路径看起来像这样：`addons/custom_node`

插件可以通过 UI 分发和安装。
如果你制作一个包含该文件夹的 zip 压缩包，Godot 会识别它
为插件并允许你安装。

可以通过终端执行：`zip -r custom_node.zip custom_node/*`
